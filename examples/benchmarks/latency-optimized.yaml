# Benchmark config: Low-latency configuration
# Use case: Measure latency with low batching

appid: "benchmark-latency"
bootstrap: "localhost:9092"
input: "test-input"
offset: "latest"
threads: 2  # Fewer threads for lower contention

# Performance tuning for low latency
performance:
  fetch_min_bytes: 1          # Don't wait for data
  fetch_max_wait_ms: 10       # Very short wait
  queue_buffering_max_ms: 1
  batch_size: 100             # Small batches
  linger_ms: 0                # Send immediately

# Auto commit keeps this diagnostic profile focused on processing latency.
commit_strategy:
  manual_commit: false

# No retry for latency test
retry:
  max_attempts: 1
  initial_delay_ms: 1
  max_delay_ms: 10
  jitter: 0.0

# DLQ disabled
dlq:
  enabled: false

# Passthrough routing
routing:
  routing_type: "passthrough"
  destinations:
    - output: "test-output"
      partitioning: "default"
      preserve_timestamp: true

observability:
  metrics_enabled: true
  metrics_port: 9090
